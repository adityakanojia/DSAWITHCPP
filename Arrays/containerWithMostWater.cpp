#include<iostream>
#include<vector>
using namespace std;

int brute_force_solution (vector<int>& nums) {
  int largest_area = 0;
  
  for (int i = 0;i < nums.size(); i++) {
            
  }

  return largest_area;
}

int main() {
  vector<int> graph = {1,2,3,2,2,4,3,5};
  brute_force_solution(graph);
  cout << "Area to cntain most water";
  return 0;
}
